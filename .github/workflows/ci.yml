name: CI

on:
  push:
    branches: [ release ]


jobs:
  call-release-workflow:
    uses: josue-r/Pipelines/.github/workflows/release.yml@release
    secrets: inherit
    with:
      port: 4202
      tags: "thistag1"
